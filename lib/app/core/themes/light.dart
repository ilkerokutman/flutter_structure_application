import 'package:flutter/material.dart';
import 'package:flutter_structure_application/app/core/themes/base.dart';

final lightTheme = BaseTheme.baseTheme(Brightness.light).copyWith(
  primaryColor: Colors.blue,
);
